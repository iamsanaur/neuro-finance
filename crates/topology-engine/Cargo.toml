[package]
name = "topology-engine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "Dynamic topology learner: sparse top-k attention-scored adjacency, persistence (EMA), sparsity/stability/relation regularization."

[dependencies]
