[package]
name = "cli"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "neurofinance CLI: data ingest/validate, features build, graph build, train, evaluate, backtest, topology inspect, serve, experiment run."

[[bin]]
name = "neurofinance"
path = "src/main.rs"

[dependencies]
serde = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }
clap = { workspace = true }
