[package]
name = "feature-engine"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "Causal, point-in-time-safe feature computation (returns, vol, momentum, rolling stats). No centered windows, no future data."

[dependencies]
financial-types = { workspace = true }

[dev-dependencies]
rand = { workspace = true }
rand_chacha = { workspace = true }
data-engine = { workspace = true }
chrono = { workspace = true }
