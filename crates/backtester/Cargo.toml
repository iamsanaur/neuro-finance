[package]
name = "backtester"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "Prediction-stream to portfolio-return backtest engine with transaction costs, slippage, turnover, and liquidity constraints modeled explicitly."

[dependencies]
