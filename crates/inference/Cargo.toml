[package]
name = "inference"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "Low-latency inference service wrapping a trained model for prediction serving. Planned for V0.2."

[dependencies]
